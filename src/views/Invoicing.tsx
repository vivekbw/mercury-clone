import React from "react";
import { Typography, Box } from "@mui/material";

export function Invoicing() {
  return (
    <Box sx={{ padding: "15px" }}>
      <Typography fontSize={20}>Invoicing</Typography>
    </Box>
  );
}
